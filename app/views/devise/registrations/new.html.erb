<div class="row vertical-offset-100">
      <div class="col-md-4 col-md-offset-4">
<div class="row">
  <div class="span4">

  <h1>

  </h1>

  <h1>

  </h1>

  <h1>

  </h1>

  <h1>
    
  </h1>
  <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: {class: "well"}) do |f| %>


    <fieldset>
      <h1 class="text-center login-title"><strong> Sign Up!</strong> </h1> 
      <%= f.input :first_name, placeholder: "First Name", label: false %>
      <%= f.input :last_name, placeholder: "Surname", label: false %>
      <%= f.input :email, placeholder: "Email", label: false %> 
      <%= f.input :password, placeholder: "Password", label: false %>
      <%= f.input :password_confirmation, placeholder: "Re-enter Password", label: false %>
          <div><%= f.button :submit, "Get In Fighter!", class: "btn btn-lg btn-info btn-block" %></div>
      <% end %>
      <div class="text-center">
        <p>
        </p>
        <h1>
        </h1>
        <%- if controller_name != 'sessions' %>
  <strong><%= link_to "Have an account! Login", new_session_path(resource_name) %></strong><br />
<% end %>
</div>
</fieldset>
          <script type="text/javascript" src="/assets/jquery.backstretch.min.js"></script>
    <script>
        $.backstretch("/assets/signup.jpg", {speed: 500});
    </script>

    
